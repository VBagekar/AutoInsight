fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0
pandas>=2.1.0
openpyxl>=3.1.0
numpy>=1.26.0
# TODO: duckdb is not currently imported. Keep if large-dataset SQL querying via DuckDB is planned;
# remove once confirmed unused.
duckdb>=0.9.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
# openai>=1.12.0 — timeout= and max_retries= constructor kwargs are available since 1.0.0;
# current minimum version is sufficient.
openai>=1.12.0
# python-dotenv is NOT needed: pydantic-settings loads .env natively via env_file in config.py.
# Kept for one release cycle in case any external script depends on it; safe to remove after audit.
# python-dotenv>=1.0.0
requests>=2.31.0
