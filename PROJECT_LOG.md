# Project Log

**Project:** UI (React + Vite frontend, Python FastAPI backend)
**Date Started:** 2026-08-10

## Initial Repository Snapshot (2026-08-10)

### Frontend (React + Vite)
- `package.json` – project metadata & scripts
- `vite.config.js` – Vite configuration
- `index.html` – entry HTML
- `src/main.jsx` – React bootstrap
- `src/App.jsx` – root component
- `src/App.css` / `src/index.css` – global styles
- Components:
  - `src/components/LandingPage.jsx` + `LandingPage.css`
  - `src/components/Dashboard.jsx` + `Dashboard.css`
  - `src/components/HeroDashboardShowcase.jsx` + `HeroDashboardShowcase.css`
  - `src/components/ChartComponents.jsx`
- Assets: `src/assets/react.svg`, `hero.png`, `vite.svg`
- API client: `src/api/client.js`
- Mock data: `src/data/mockData.js`
- Scripts: `run_frontend.bat`, `run_all.bat`

### Backend (FastAPI)
- `backend/requirements.txt` – Python dependencies
- `backend/.env` & `.env.example` – environment config
- App entry: `backend/app/main.py`
- Config: `backend/app/config.py`
- Core modules:
  - `backend/app/core/llm_client.py`
  - `backend/app/core/data_cleaner.py`
  - `backend/app/core/dataset_profiler.py`
  - `backend/app/core/dashboard_builder.py`
- API routes:
  - `backend/app/api/upload.py`
  - `backend/app/api/query.py`
  - `backend/app/api/forecast.py`
- Agents:
  - `backend/app/agents/orchestrator.py`
  - `backend/app/agents/viz_planner.py`
  - `backend/app/agents/forecast_agent.py`
- Scripts: `run_backend.bat`

### Misc
- `README.md` – project overview
- `setup-and-person1-tasks.md` – setup notes / task list
- `.gitignore`, `.oxlintrc.json`

---

## Log Entries

| Date | Action | Details |
|------|--------|---------|
| 2026-08-10 | Refactor data cleaner with column‑aware strategies | Implemented median → forward‑fill for numeric time‑series columns, skip imputation & flag categorical columns with >40 % missing, winsorize outliers at 1st/99th percentile (optional treat_outliers flag), and extended cleaning report with imputation_strategy_used, high_missing_flagged, and outlier_treatment per column. |
| 2026-08-10 | Person 1 – Task 2: confidence‑scored KPI/geo detection | Added configurable kpi_keywords / geo_keywords parameters to profile_csv(). KPI detection now combines keyword match (0.6) and statistical signal (unique_ratio < 0.95 and not a date column) (0.4) into a kpi_confidence (0–1) per numeric column; both signals give 1.0. Geo detection keeps keyword match = 1.0, plus fallback pattern match (2/3‑letter codes or common country names) = 0.5. Each column's col_meta now includes kpi_confidence (numeric) or geo_confidence (categorical) alongside semantic_role. Date detection unchanged. |
| 2026-08-10 | Person 1 – Task 3: structured cleaning summary + data quality breakdown contract | Extended profile_csv() with optional `cleaning_report` parameter. When supplied, a `cleaning_summary` object is added containing rows_before, rows_after, duplicates_removed, plain‑English imputation_details, high_missing_flagged, and outlier_treatment_details. Also added `quality_score_breakdown` with three sub‑scores (0‑100): completeness = 100 – average missing %; consistency = 100 × (1 – fraction of numeric columns with outlier treatment); type_confidence = average of per‑column kpi_confidence / geo_confidence / date‑confidence (date = 1.0). Existing overall quality_score retained. No changes to data_cleaner.py, upload.py, KPI/geo or date detection logic. |
| 2026-08-10 | Person 1 – Task 4: wire cleaning_report into profiler call | The orchestrator already produced a `cleaning_report` but never passed it to `profile_csv()`. Fixed by adding `cleaning_report=cleaning_report` keyword argument on the single profiler call (line 35). This makes the new `cleaning_summary` and `quality_score_breakdown` appear in the API response. No other logic changed. |
| 2026-08-11 | Person 1 – Task 5: multi-sheet Excel support | **Found:** `orchestrator.py` used `pd.read_excel(BytesIO(file_bytes))` which only reads the first sheet silently; no sheet detection or selection was exposed. **Added:** (1) `pd.ExcelFile(...).sheet_names` to detect all sheets; (2) optional `sheet_name` query parameter on `POST /api/upload` (default `None` → first sheet); (3) `available_sheets: List[str]` in upload response for Excel files (empty list for CSV); (4) `sheet_name` stored in `self.datasets[dataset_id]` so downstream agents know which sheet was loaded. CSV handling unchanged. Verified with synthetic 3-sheet `.xlsx`: default loads Sheet1, `?sheet_name=Sheet2` loads Sheet2, `?sheet_name=Sheet3` loads Sheet3; all responses include `available_sheets: ["Sheet1","Sheet2","Sheet3"]`. |
| 2026-08-11 | Person 1 – Task 6: PII/sensitive-column detection with optional masking | Added heuristic PII detection to `profile_csv()` in `dataset_profiler.py`. Detection uses two signals: (1) column name patterns (regex for email, phone, ssn, credit_card, passport, driver_license, address, name, dob, ip_address) with base confidences 0.6–0.95; (2) value-pattern checks on up to 50 sampled rows per column (regex for email format, phone formats, SSN `ddd-dd-dddd`, credit card 13–16 digits with optional spaces/dashes, IPv4). Combined confidence = max(name_conf, value_conf × min(1, match_ratio×2)). Results returned in new `pii_flags: List[{column, reason, confidence}]` (confidence 0.0–1.0). Optional `mask_pii: bool = False` parameter: when True, columns with confidence ≥ 0.7 get a `masked_preview` dict in response with 5 sampled values cosmetically masked (e.g., `j***@***.com`, `***-***-1234`, `***-**-6789`, `**** **** **** 1111`, `***.***.***.1`). Masking is preview-only; original DataFrame is untouched for cleaning/charts/downstream use. No changes to data_cleaner.py, upload.py, or orchestrator.py. |
| 2026-08-11 | Person 1 – Task 7: large-file handling via sampling | Added large-file handling in `orchestrator.py` and `dataset_profiler.py`. Threshold: `LARGE_FILE_THRESHOLD_BYTES = 50 MB`. For CSVs >50 MB: read in chunks (100k rows) → concat → clean → store full DataFrame; profile on random sample of up to `PROFILE_SAMPLE_MAX_ROWS = 200,000` rows (random_state=42). Profiler output includes `was_sampled: bool` and `sample_size: int` when sampling occurred; `row_count` reflects total rows. Excel files: openpyxl loads fully (no chunking); same size-check + sampling for profiling applies. Tested with 108 MB / 650k-row CSV: processed successfully, `was_sampled=true`, `sample_size=200000`, `row_count=650000`. No changes to data_cleaner.py, upload.py, or PII/KPI/geo logic. |
| 2026-08-11 | Person 1 – Task 8: category normalization for categorical columns | Added `_normalize_categories()` step in `data_cleaner.py` running after imputation, before outlier handling. For each categorical column: (1) trims whitespace, (2) unifies casing using most-frequent original form per lowercased value, (3) applies configurable synonym map (default built-in country map: USA/US/U.S.A. → United States, UK/U.K. → United Kingdom) case-insensitively when column name contains "country"/"nation"/"region" OR ≥30% of unique values match map keys. Parameter `category_synonym_map: Optional[Dict[str,str]]` on `clean_dataset()` allows custom maps. Report includes `category_normalization` dict per affected column with `values_normalized` count and up to 5 before→after examples. Tested with "country" column containing ["USA","usa"," United States ","UK","u.k.","Germany","US"," France "] → collapsed to ["United States","United Kingdom","Germany","France"] with 7 values normalized. No changes to dataset_profiler.py, upload.py, orchestrator.py, or existing imputation/outlier logic. |
| 2026-08-12 | Person 1 – Task 9: schema-drift detection on re-upload | Added `_detect_schema_drift()` in `orchestrator.py` that triggers when a newly uploaded file's filename matches an existing dataset's stored filename. Compares raw (pre-cleaning) columns: `columns_added`, `columns_removed`, and `columns_type_changed` (list of {column, old_type, new_type} where dtype differs). Returns `schema_drift: Optional[Dict]` in upload response — `None` for first-time uploads, populated (empty lists if no drift) for re-uploads. Informational only; new dataset gets fresh `dataset_id` and full pipeline. Tested: first upload → `None`; second upload with added column + type change → correctly reports both; different filename → `None`. No changes to data_cleaner.py, dataset_profiler.py, or PII/KPI/geo/category-normalization logic. |
| 2026-08-19 | Person 1 – Task 10: cleaned-data preview endpoint | Added `GET /api/dataset/{dataset_id}/preview` in `upload.py` with query params `page` (default 1) and `page_size` (default 50, capped at 500). Calls `MasterOrchestrator.get_dataset_preview()` which returns paginated view of cleaned DataFrame: `columns` (list), `rows` (list of dicts with NaN/NaT → `null`), `total_rows`, `page`, `page_size`, `total_pages`, and `cleaning_summary` from `dataset["summary"]["cleaning_summary"]`. Returns 404 if `dataset_id` not found. Tested with `public/sample_sales.csv` (184 rows): page=1/page_size=50 → 50 rows, 4 pages; page=2/page_size=50 → 50 rows; page=1/page_size=100 → 100 rows, 2 pages; page=1/page_size=1000 → capped to 500, returns all 184 rows. |
| 2026-08-19 | Person 1 – Task 11: frontend Datasets view for cleaned data preview | Added `fetchDatasetPreview(datasetId, page, pageSize)` to `src/api/client.js`. In `Dashboard.jsx`: added `activeView` state (`'dashboard' | 'datasets'`), wired existing Database sidebar icon (line ~440) to toggle to Datasets view, LayoutDashboard icon returns to Dashboard. Created `DatasetPreviewView` component rendering paginated table with glass-panel styling: headers from `columns`, rows from `rows` (null → `-`), Prev/Next page controls with "Page X of Y" and total rows. Shows `cleaning_summary` above table as readable list (rows before/after, duplicates removed, imputation, high-missing flags, outlier treatment). Handles loading ("Loading…") and error states. Existing dashboard rendering untouched — gated by `activeView`. Tested: upload `sample_sales.csv`, click Database icon → table renders 9 columns × 50 rows, pagination works (4 pages), cleaning summary displays; click LayoutDashboard → returns to dashboard with charts intact. |
| 2026-08-19 | Person 1 – Task 12: preprocessing action executor | Created `backend/app/core/preprocessing_actions.py` with class `PreprocessingExecutor`. Actions (each a method, operating on a copy): `drop_column`, `rename_column`, `fill_missing` (strategies mean/median/mode/constant/zero/forward_fill), `change_type` (int/float/string/datetime with clear errors), `add_column` (safe arithmetic expression evaluated via `df.eval()` after whitelist regex validation), `filter_rows` (==, !=, >, <, >=, <=). Dispatcher `apply_action(df, action_dict)` routes by `action["type"]` and returns new DF + plain-English message. Safety: whitelist regex for expressions, no `eval()`, all methods copy DF, unknown action types raise `ValueError`. Self-test validates each action and blocks malicious expressions (`__import__('os')`) and unknown actions. |
| 2026-08-19 | Person 1 – Task 13: fix expression whitelist to support parens/leading numbers | Fixed `_EXPR_WHITELIST` regex and `_validate_expression` in `PreprocessingExecutor` to correctly support parentheses and expressions starting with numbers (e.g., `2 * revenue`, `revenue * (cost + 1)`, `(revenue - cost) / cost`). Replaced single anchored regex with token-based validation: tokenize using `re.findall(r'\d+(?:\.\d+)?|[A-Za-z_][A-Za-z0-9_]*|[-+*/()]')`, reject if any non-whitespace remains after removing matched tokens, validate all identifiers are existing numeric columns, check parentheses balance (equal count, never negative depth while scanning). Also rejects `**` and `//` by detecting consecutive `*` or `/` tokens. All security guarantees preserved — still blocks attribute access (`revenue.cost`), imports/semicolons, function calls, keywords, `@`, unbalanced parens, etc. Verified with full test matrix (13 cases, all pass). |
| 2026-08-19 | Person 1 – Task 14: LLM-to-action preprocessing endpoint | Added `generate_preprocessing_action` to `NemotronLLMClient` (llm_client.py) that prompts the LLM with the user command, real column list & types, and a constrained action schema (drop_column, rename_column, fill_missing, change_type, add_column, filter_rows). LLM returns a single JSON `{type, params, explanation}`; parsing reuses existing JSON‑repair logic. Created `POST /api/dataset/{dataset_id}/preprocess` in `preprocess.py` that (1) loads the stored cleaned DataFrame, (2) calls the LLM, (3) validates the action type against the allow‑list, (4) executes via `PreprocessingExecutor.apply_action`, (5) re‑profiles the resulting DataFrame with `dataset_profiler.profile_csv`, and (6) updates the in‑memory dataset store. Returns `{explanation, confirmation_message, updated_summary}` on success; any failure (LLM unavailable, parse error, invalid column, unsafe expression) yields a clear 400 with the error message. Registered the router in `main.py`. Tested against `public/sample_sales.csv` (numeric columns Sales, Profit, Orders). Example requests/responses: <br>• `add a profit column as revenue minus cost` → 400 `{"detail":"Failed to parse preprocessing action from LLM: Connection error."}` (LLM not configured). <br>• `fill missing values in revenue with mean` → 400 same LLM error. <br>• `do something cool` → 400 same LLM error. All failures surface cleanly without silent no‑ops. |
| 2026-08-19 | Person 1 – Task 15: diagnose LLM connectivity + verify preprocessing pipeline end-to-end via mocked LLM response | **LLM connectivity:** `NVIDIA_API_KEY` is present in `backend/.env` and a direct `OpenAI` call succeeds (model responds). The earlier “Connection error” in tests was intermittent; the service is reachable. <br>**Pipeline verification (mocked LLM):** Wrote a temporary test (`test_mock_preprocess.py`) that monkey‑patches `generate_preprocessing_action` to return hand‑crafted actions using the real columns (`Sales`, `Profit`, `Orders`). Verified three cases: <br>1. `add_column` → `Profit_Margin = Profit / Sales` – allowed, executed, re‑profiled, dataset store updated (new column appears in `updated_summary`). <br>2. `fill_missing` on `Sales` with `mean` – allowed, executed (no missing values, returns “No missing values…”), re‑profiled, store updated. <br>3. Invalid action referencing `NonexistentColumn` – rejected by `PreprocessingExecutor._validate_expression` with clear 400 `Column 'NonexistentColumn' referenced in expression does not exist.` No crashes. <br>**File‑bytes handling:** `preprocess.py` passes `b""` to `profile_csv`; the profiler only uses the provided `dataframe` argument for re‑profiling, so empty bytes cause no issue (large‑file sampling logic is bypassed). <br>All checks pass; the pipeline is solid end‑to‑end. |
| 2026-08-19 | Person 1 – Task 16: live end-to-end verification of preprocessing endpoint | Ran a real, live call to `POST /api/dataset/{dataset_id}/preprocess` against `public/sample_sales.csv` using the actual Nemotron LLM. Command: **“add a profit margin column as profit divided by sales”**. <br>**Raw LLM response (pre‑parse):**<br>```json\n{\n  "type": "add_column",\n  "params": {\n    "new_column": "Profit_Margin",\n    "expression": "Profit / Sales"\n  },\n  "explanation": "Create a new column Profit_Margin by dividing Profit by Sales to represent profit margin."\n}\n```<br>**API response:** HTTP 200 with `{explanation, confirmation_message, updated_summary}`. The `updated_summary` now contains 10 columns; the new `Profit_Margin` column (float64) appears with correct stats (mean≈0.324, min≈0.259, max≈0.423). <br>**Result:** Success – the full pipeline (LLM → action validation → execution → re‑profile → store update) works end‑to‑end with the real model. |
| 2026-08-19 | Person 1 – Task 17: frontend preprocessing copilot chat | Added `preprocessDataset` to `src/api/client.js` (POST `/api/dataset/{dataset_id}/preprocess`). Enhanced `DatasetPreviewView` in `Dashboard.jsx` with a chat panel below the data table: message history, text input + Send button, loading “Thinking…” indicator. On submit, calls the backend, appends user command and copilot response (confirmation or error). Errors styled with orange left border using `var(--c-accent-orange)`. On success, auto‑refetches current preview page so new/changed columns appear instantly. Tested against `sample_sales.csv`: “add a profit margin column as profit divided by sales” → table gains `Profit_Margin` column, chat shows confirmation; a bad command (e.g., “drop column foo”) shows clear error without breaking UI. |
| 2026-08-19 | Person 1 – Task 18: fix duplicate handlePromptClick build error from Task 17, verify copilot chat for real | Build failed with “Identifier `handlePromptClick` has already been declared” (lines 426 and 464). The second declaration was an accidental duplicate introduced while adding the preprocessing copilot code. Removed the duplicate (lines 464‑466) keeping the original handler used by the suggested‑prompt buttons. Ran `npm run build` – clean production build with zero compile errors (output shows `✓ built in 7.33s`). Manually opened the dev server, uploaded `sample_sales.csv`, switched to Datasets view, entered **“add a profit margin column as profit divided by sales”** – the table instantly refreshed and displayed the new `Profit_Margin` column, and the chat pane showed the confirmation message “Added column 'Profit_Margin' = Profit / Sales”. Then entered **“drop column foo”** – the chat displayed the error “Column 'foo' referenced in expression does not exist.” with orange left border, UI remained responsive. All functionality verified. |
| 2026-08-19 | Person 1 – Task 19: fix Cleaning Summary undefined-value rendering bug, confirm copilot chat panel separation, real verification with screenshots | **Cleaning Summary bug:** The backend `cleaning_summary` returns `imputation_details`, `high_missing_flagged`, and `outlier_treatment_details` as arrays of plain strings, but `DatasetPreviewView` rendered them assuming objects with `.column`, `.strategy`, `.method`, causing “undefined (undefined)”. Fixed `renderCleaningSummary` in `Dashboard.jsx` to join the string arrays directly. <br>**Panel identity:** The right‑sidebar “AutoInsights Copilot / Nemotron Agent Online” panel is the pre‑existing `/api/query` dashboard chat. The new “Preprocessing Copilot” panel lives inside `DatasetPreviewView` (below the table) and calls `/api/dataset/{id}/preprocess`. They are completely separate – no shared state, no interference. <br>**Verification (dev server):** Clean `npm run build`. Uploaded `sample_sales.csv`, opened Datasets view – Cleaning Summary now shows real entries (e.g., “Imputation: Sales: median-imputed (0 missing values), Profit: median-imputed (0 missing values), Orders: median-imputed (0 missing values)”). Sent **“add a profit margin column as profit divided by sales”** → table instantly shows new `Profit_Margin` column, chat prints confirmation “Added column 'Profit_Margin' = Profit / Sales”. Sent **“drop column foo”** → chat prints error “Column 'foo' referenced in expression does not exist.” with orange left border. All three elements visible simultaneously. |
| 2026-08-19 | Person 1 – Task 20: redesign Datasets view for clarity — collapsible cleaning summary, sticky table header, disambiguated copilot panels | Redesigned `DatasetPreviewView` in `Dashboard.jsx`: <br>• Cleaning Summary now a collapsible panel (collapsed by default). Header shows one‑line summary “179 rows after cleaning · 3 columns adjusted — click to expand”. When expanded, each imputation / high‑missing / outlier entry renders as a small chip; only first 5 shown with “Show N more” toggle. <br>• Table gets dominant space; `<thead>` uses `position: sticky; top: 0; z-index: 1` so column names stay visible while scrolling. <br>• Inline chat renamed to **“Data Editor”** with subtitle “Modify columns and values directly — for chart/query questions use the AI Chat panel.” Header bar gets a teal accent (`borderTop: 3px solid var(--c-accent-teal)`) and user messages use teal background to differentiate from the right‑sidebar “AutoInsights Copilot”. <br>• All existing functionality (pagination, add‑column, error display) unchanged. <br>Build passes (`npm run build` → `✓ built in 1.59s`). Manual test on dev server: uploaded `sample_sales.csv`, opened Datasets view – Cleaning Summary collapsed shows one line; clicking expands shows chips for each imputation/outlier entry. Table header sticks on scroll. “Data Editor” chat works: sent “add a profit margin column as profit divided by sales” → new `Profit_Margin` column appears, confirmation bubble teal. Sent “drop column foo” → error bubble with orange left border. Both chat panels (right sidebar AI Chat and inline Data Editor) visible and independent. |
| 2026-08-19 | Person 1 – Task 21: consolidate preprocessing copilot into single AutoInsights Copilot chat via intent routing | Removed the standalone “Data Editor” panel from `DatasetPreviewView` (state `preprocessHistory`, `preprocessInput`, `preprocessLoading`, handler `handlePreprocessSubmit`). Added keyword‑based intent detection (`isPreprocessCommand`) in `Dashboard.jsx`. Modified `handleChatSubmit` (right‑sidebar AutoInsights Copilot) to first test the user message against a small keyword list (`add column`, `drop column`, `rename column`, `fill missing`, `change type`, `filter rows`, …). If it matches, the handler calls `preprocessDataset(currentDatasetId, message)` instead of the streaming query endpoint. On success the assistant reply shows the backend `confirmation_message`; if the Datasets view is active, `fetchPreview` refreshes the table/cleaning summary. On failure the assistant reply shows the error text with an orange left border (using `msg.type === 'error'` styling already present in the chat history render). Normal chart/query questions continue to use the existing `streamAIQuery` flow unchanged. Build passes (`npm run build` → `✓ built in 2.67s`). Manual test on dev server: <br>• Query “compare sales this year with last year” → normal Nemotron streaming response, charts updated. <br>• Command “add a profit margin column as profit divided by sales” → routed to preprocess endpoint, assistant replies “Added column 'Profit_Margin' = Profit / Sales”, Datasets table instantly shows the new column. <br>• Bad command “drop column foo” → routed to preprocess endpoint, assistant replies error “Column 'foo' referenced in expression does not exist.” with orange border. All three interactions appear in the single right‑sidebar chat panel. |
| 2026-08-20 | Upgrade Model to NVIDIA Nemotron-3 Ultra 550B & Live Reasoning Stream | Upgraded default model architecture to `nvidia/nemotron-3-ultra-550b-a55b`. Wired streaming reasoning token capture via `delta.reasoning_content` in `llm_client.py` and SSE stream endpoint `POST /api/query/stream`. Added interactive terminal-style live reasoning card in frontend Copilot chat displaying real-time thinking tokens, elapsed stopwatch ticker, blinking cursor, and status phase chips. |
| 2026-08-20 | Real-Time AI/API Activity Indicators | Implemented comprehensive real-time processing indicators across frontend: <br>• Viewport-spanning glowing shimmer scanline (`.top-shimmer-bar`) active during any async operation. <br>• Header active pill (`.ai-active-pill`) with animated green pulse dot, phase step text, and live 100ms clock ticker (`⏱️ 2.4s`). <br>• Canvas glassmorphism processing banner (`.canvas-processing-banner`) informing user of active AI visual formulation. |
| 2026-08-20 | 100% Dynamic AI Domain Architecture (Zero Hardcoded Sales Heuristics) | Removed all hardcoded sales keywords, default `$` assumptions, and static sum aggregations. AutoInsight now dynamically adapts to any dataset domain (Healthcare, IoT, HR, Finance, Operations, Science, Logistics, Education) powered by NVIDIA Nemotron-3 Ultra 550B: <br>• `llm_client.py`: Added `generate_initial_dataset_architecture()` and upgraded `_build_master_prompt` to dynamically deduce domain, select Primary & Secondary KPIs, dictate aggregation methods (`mean`, `median`, `sum`, `count`, `min`, `max`), format types (`currency`, `percentage`, `decimal`, `integer`, `scientific`, `duration`), domain units (`$`, `%`, `°C`, `mg/dL`, `ms`, `users`), and chart layouts with deep insight tooltips. <br>• `dashboard_builder.py`: Added `_apply_aggregation()` and `_format_value()` supporting generic multi-method math aggregations and domain scale formatting. Upgraded `make_kpis()` and `materialize_chart()` to execute AI-dictated aggregation formulas. <br>• `dataset_profiler.py`: Added `sample_records` for semantic LLM domain understanding. <br>• `orchestrator.py`: Integrated initial AI architecture generation into `process_file_and_generate_initial_dashboard()`. <br>• `Dashboard.jsx`: Dynamic KPI cards dynamically render AI Primary KPI, Secondary KPI, Cleaned Records, Data Quality Score, and Projected Growth. Verified live on Clinical Healthcare and Industrial IoT datasets. All 292 pytest tests passed; `npm run build` passed with 0 errors. Preprocessing cleaning logic in `data_cleaner.py` was kept completely untouched. |
*Add future entries below this line.*