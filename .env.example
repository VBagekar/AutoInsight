# Frontend environment variables — copy this to .env in the project root for production.
# NEVER put real secrets here — this file is committed to source control.

# Set this to your deployed backend URL in production.
# Leave the default for local development.
VITE_API_BASE_URL=http://localhost:8000/api
